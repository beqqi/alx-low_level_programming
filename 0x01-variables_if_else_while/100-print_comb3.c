#include <stdlib.h>
#include <stdio.h>
/**
 *main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
for (int i = 10; i < 100; i++)
{
putchar(i);
putchar(',');
putchar("\n");
}
return (0);
}
